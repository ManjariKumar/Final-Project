
<%= form_for(@user) do |f| %>
	<% if @user.errors.any? %>
		<div class="error-explanation">
		 <h2> <%= pluralize(@user.errors.count, "error")%> prevented this user from being saved </h2>

		 <ul>
			 <% @user.errors.full_messages.each do |msg| %>
			   <li><%= msg %></li>
			 <%end%>
		 </ul>
		</div>
	<%end%>

	<p>
     	<%= f.label "User Email" %><br>
	  	<%= f.text_field :email %><br>
    </p>

	<p>
     	<%= f.label "User Password" %><br>
	  	<%= f.text_field :encrypted_password %><br>
    </p>

    <p>
     	<%= f.label "Role"%><br>
	  	<%= f.text_field :role %><br>
    </p>

<%= f.submit %>
<%end%>

<h4><%= link_to "Back", users_path %></h4>




